# 隐私说明 · apkgo 助手

**apkgo 助手只做一件事：把你在应用商店开放平台后台看到的 API 密钥，按你的操作保存到你自己的 apkgo cloud 组织里。**

## 收集什么

- 你在后台页面上主动点「自动识别」「点选」或选择的文件所得到的密钥字段。
- 你在 apkgo 网页上点「连接助手」时，apkgo 签发给扩展的一把访问令牌（权限仅为新建商店账号）。

除此之外不收集任何数据：不记录浏览历史，不读取与密钥无关的页面内容，不采集个人信息，没有统计埋点。

## 发到哪里

- 只发往你连接时所在的 apkgo 地址（默认 `https://apkgo.baici.tech`），通过 HTTPS。
- 不发往任何第三方服务器。扩展代码里不存在其他网络地址。

## 存在哪里

- 商店密钥：仅存于浏览器会话存储（`chrome.storage.session`），关闭浏览器即清除；保存到 apkgo 成功后立即清除。
- apkgo 访问令牌：存于扩展的本地存储，用于后续保存操作。你可以在扩展弹窗里点「断开连接」删除，或在 apkgo「API 密钥」页吊销。

## 权限说明

- `storage`：保存上述两类数据。
- `scripting`：在商店后台页面里挂一个下载钩子，页面下载密钥文件时把内容直接填进面板，省掉文件对话框。钩子只读页面自己发起的下载，不改变页面行为。
- `downloads`：识别刚发生的密钥文件下载（文件名为 .json / .cer / .pem / .p8），配合上一条。不读取你的其他下载。
- 各商店后台域名：在这些页面上显示引导面板并读取你指定的密钥字段。
- apkgo 域名：让 apkgo 网页能把令牌交给扩展。
- 可选的其他域名：仅当你把扩展连接到私有化部署的 apkgo 地址时，由你手动授权。

## 开源与核验

源码在 https://github.com/KevinGong2013/apkgo-assistant ，MIT 许可。Release 提供可复现构建的 zip 和 sha256，商店上架的版本与之一致。

## 联系

问题或疑虑请在仓库提 issue。
